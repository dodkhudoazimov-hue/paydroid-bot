import telebot
import os
import subprocess
import requests
import yt_dlp
import tempfile

# >>>>>> ТУ ДАР ИН ҶО TOKEN-И ХУДРО ГУЗОР <<<<<<
BOT_TOKEN = "8238563485:AAHNLTZodPeXcl7YfjZqIqY6BpcPuP3QGXs"
AUDD_API_TOKEN = "67bb96ec3f8a31cf3b9a73a4279fbb99"

bot = telebot.TeleBot(BOT_TOKEN)

# /start команда
@bot.message_handler(commands=['start', 'help'])
def send_welcome(message):
    bot.reply_to(
        message,
        "🎵 Салом! Ман мусиқиро аз овоз, видео ё линкҳо мешиносам.\n"
        "Фақат овоз ё линки TikTok / Instagram / YouTube фиристед 🎧"
    )

# === ФУНКСИЯИ ЁРДАМӢ: ба AudD мефиристад ва натиҷа мегирад ===
def recognize_music(file_path):
    with open(file_path, 'rb') as f:
        resp = requests.post(
            'https://api.audd.io/',
            data={'api_token': AUDD_API_TOKEN, 'return': 'apple_music,spotify,lyrics'},
            files={'file': f}
        )
    data = resp.json()
    if data.get("status") != "success" or not data.get("result"):
        return None
    r = data["result"]
    text = f"🎵 <b>{r.get('title','?')}</b> — {r.get('artist','?')}"
    if r.get("album"):
        text += f"\n💿 Альбом: {r['album']}"
    if r.get("release_date"):
        text += f"\n📅 Санаи нашр: {r['release_date']}"
    if r.get("spotify") and r["spotify"].get("external_urls"):
        text += f"\n🎧 Spotify: {r['spotify']['external_urls'].get('spotify')}"
    if r.get("apple_music") and r["apple_music"].get("url"):
        text += f"\n🍎 Apple Music: {r['apple_music']['url']}"
    if r.get("lyrics") and r["lyrics"].get("lyrics"):
        lyr = r["lyrics"]["lyrics"][:400]
        text += f"\n📝 Lyrics:\n{lyr}..."
    return text

# === ФУНКСИЯ: зеркашии видео ва гирифтани аудио аз линк ===
def download_audio_from_url(url):
    tmpdir = tempfile.mkdtemp()
    out_path = os.path.join(tmpdir, "audio.%(ext)s")
    opts = {
        'format': 'bestaudio/best',
        'outtmpl': out_path,
        'quiet': True,
    }
    try:
        with yt_dlp.YoutubeDL(opts) as ydl:
            info = ydl.extract_info(url, download=True)
            filename = ydl.prepare_filename(info)
            # Табдил ба WAV
            wav_path = os.path.join(tmpdir, "audio.wav")
            subprocess.run(['ffmpeg', '-y', '-i', filename, '-ar', '44100', '-ac', '1', wav_path],
                           stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
            return wav_path
    except Exception as e:
        print("Error:", e)
        return None

# === ҲОЛАТ: агар паём линк бошад ===
@bot.message_handler(func=lambda msg: 'http' in msg.text)
def handle_link(message):
    url = message.text.strip()
    bot.reply_to(message, "🔎 Линк ёфт шуд, зеркашӣ ва таҳлил мешавад...")
    audio_file = download_audio_from_url(url)
    if not audio_file:
        bot.send_message(message.chat.id, "❌ Натавонистам аудиоро аз ин линк гирам.")
        return
    result = recognize_music(audio_file)
    if result:
        bot.send_message(message.chat.id, result, parse_mode='HTML')
    else:
        bot.send_message(message.chat.id, "❌ Мусиқӣ ёфт нашуд.")

# === ҲОЛАТ: агар паёми овозӣ ё аудио бошад ===
@bot.message_handler(content_types=['voice', 'audio', 'video_note'])
def handle_voice(message):
    file_info = bot.get_file(message.voice.file_id if message.voice else message.audio.file_id)
    downloaded = bot.download_file(file_info.file_path)
    tmpdir = tempfile.mkdtemp()
    ogg_path = os.path.join(tmpdir, "voice.ogg")
    with open(ogg_path, 'wb') as f:
        f.write(downloaded)

    wav_path = os.path.join(tmpdir, "voice.wav")
    subprocess.run(['ffmpeg', '-y', '-i', ogg_path, '-ar', '44100', '-ac', '1', wav_path],
                   stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)

    bot.reply_to(message, "🎧 Шинохти мусиқӣ оғоз ёфт, лутфан интизор шавед...")
    result = recognize_music(wav_path)
    if result:
        bot.send_message(message.chat.id, result, parse_mode='HTML')
    else:
        bot.send_message(message.chat.id, "❌ Мусиқӣ ёфт нашуд.")

# === Иҷрои бот ===
print("✅ Бот кор мекунад...")
bot.infinity_polling()
