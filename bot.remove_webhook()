from telegram import InlineKeyboardButton, InlineKeyboardMarkup, Update
from telegram.ext import ApplicationBuilder, CallbackQueryHandler, CommandHandler, ContextTypes

# Ҳолати бозӣ барои ҳар chat
games = {}

# Сохтани ҷадвал
def create_board(size):
    return [[" " for _ in range(size)] for _ in range(size)]

# Намоиши ҷадвал бо emoji
def render_board(board):
    symbol_map = {"X": "❌", "O": "⭕", " ": "⬜"}
    return "\n".join("".join(symbol_map[cell] for cell in row) for row in board)

# Тугмаҳои inline барои ҷадвал
def board_buttons(board, chat_id):
    buttons = []
    for i, row in enumerate(board):
        btn_row = []
        for j, cell in enumerate(row):
            if cell == " ":
                btn_row.append(InlineKeyboardButton("⬜", callback_data=f"{chat_id}:{i},{j}"))
            else:
                btn_row.append(InlineKeyboardButton(cell, callback_data="ignored"))
        buttons.append(btn_row)
    return InlineKeyboardMarkup(buttons)

# Тугмаи профили админ
def admin_button():
    return InlineKeyboardMarkup([[InlineKeyboardButton("Профили админ", url="tg://user?id=8377215874")]])

# Тафтиши бурд
def check_win(board, player):
    size = len(board)
    for i in range(size):
        if all(board[i][j] == player for j in range(size)) or all(board[j][i] == player for j in range(size)):
            return True
    if all(board[i][i] == player for i in range(size)) or all(board[i][size-1-i] == player for i in range(size)):
        return True
    return False

# Оғози бозӣ
async def start_game(update: Update, context: ContextTypes.DEFAULT_TYPE):
    chat_id = update.effective_chat.id
    msg = await update.message.reply_text(
        "Бозӣ оғоз шуд! Ду бозингар лозим аст. Дар ҷавоб навбати худро интихоб кунед (X ва O)."
    )
    # Ҳолати аввал, ҳеҷ бозингар муайян нашудааст
    games[chat_id] = {
        "board": create_board(3),
        "turn": "X",
        "players": {},  # user_id: symbol
        "message_id": msg.message_id
    }

# Callback барои click
async def button_click(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    await query.answer()
    data = query.data
    if data == "ignored":
        return

    chat_id_str, pos = data.split(":")
    chat_id = int(chat_id_str)
    i, j = map(int, pos.split(","))

    game = games.get(chat_id)
    if not game:
        await query.edit_message_text("Бозӣ ёфт нашуд. /game бозӣ кунед.")
        return

    user_id = query.from_user.id
    # Таъини бозингар
    if user_id not in game["players"]:
        if len(game["players"]) >= 2:
            await query.answer("Бозӣ барои ду бозингар аст!")
            return
        # Аввалин шахс X, дуюм O
        symbol = "X" if "X" not in game["players"].values() else "O"
        game["players"][user_id] = symbol

    # Навбати дуруст
    if game["players"][user_id] != game["turn"]:
        await query.answer("Навбати шумо нест!")
        return

    board = game["board"]
    turn = game["turn"]

    if board[i][j] != " ":
        return

    board[i][j] = turn

    # Тафтиши бурд
    if check_win(board, turn):
        await query.edit_message_text(
            f"{render_board(board)}\n{turn} бурд! 🎉",
            reply_markup=admin_button()
        )
        del games[chat_id]
        return

    # Тафтиши баробарӣ
    if all(cell != " " for row in board for cell in row):
        await query.edit_message_text(f"{render_board(board)}\nНатоиҷа баробар! 🤝",
                                      reply_markup=admin_button())
        del games[chat_id]
        return

    # Навбат
    game["turn"] = "O" if turn == "X" else "X"
    await query.edit_message_text(f"{render_board(board)}\nНавбати {game['turn']}",
                                  reply_markup=board_buttons(board, chat_id))

# Команда барои оғоз
async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text(
        "Салом! /game барои оғоз кардани бозӣ дар гуруҳ"
    )

# Запуск
if __name__ == "__main__":
    TOKEN = "8238563485:AAHNLTZodPeXcl7YfjZqIqY6BpcPuP3QGXs"
    app = ApplicationBuilder().token(TOKEN).build()
    app.add_handler(CommandHandler("start", start))
    app.add_handler(CommandHandler("game", start_game))
    app.add_handler(CallbackQueryHandler(button_click))
    print("Бот кор мекунад...")
    app.run_polling()
