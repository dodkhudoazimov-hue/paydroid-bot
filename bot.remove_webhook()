import telebot
from telebot import types

# 🔑 TOKEN-и худро гузор
TOKEN = "8238563485:AAHNLTZodPeXcl7YfjZqIqY6BpcPuP3QGXs"
bot = telebot.TeleBot(TOKEN)

# 👑 ID ё username-и админ
ADMIN_USERNAME = "@marz_x5"

# 📦 Молҳо
products = {
    1: {"name": "Телефон", "price": 1200},
    2: {"name": "Наушник", "price": 150},
    3: {"name": "Зарядник", "price": 50},
}

# 🛒 Сабадҳои корбарон
user_carts = {}
# 📬 Фармоишҳои нав барои админ
orders = []

# ----------------------------------------------------------
# Функсияҳои ёрирасон
# ----------------------------------------------------------
def make_main_menu():
    keyboard = types.InlineKeyboardMarkup()
    keyboard.add(types.InlineKeyboardButton("🛍 Дидани молҳо", callback_data="show_products"))
    keyboard.add(types.InlineKeyboardButton("🛒 Дидани сабад", callback_data="view_cart"))
    keyboard.add(types.InlineKeyboardButton("👑 Бахши админ", callback_data="admin"))
    return keyboard

def make_products_menu():
    keyboard = types.InlineKeyboardMarkup()
    for pid, p in products.items():
        keyboard.add(types.InlineKeyboardButton(f"{p['name']} - {p['price']} сомонӣ", callback_data=f"add_{pid}"))
    keyboard.add(types.InlineKeyboardButton("⬅️ Бозгашт", callback_data="back_main"))
    return keyboard

def make_cart_text(user_id):
    cart = user_carts.get(user_id, [])
    if not cart:
        return "🛒 Сабад холӣ аст."
    text = "📦 Молҳо дар сабад:\n"
    total = 0
    for p in cart:
        text += f"• {p['name']} - {p['price']} сомонӣ\n"
        total += p['price']
    text += f"\n💰 Ҷамъ: {total} сомонӣ"
    return text

# ----------------------------------------------------------
# Командаҳо
# ----------------------------------------------------------
@bot.message_handler(commands=['start'])
def start(message):
    bot.send_message(
        message.chat.id,
        f"Салом, {message.from_user.first_name}! 👋\nИн мағозаи онлайн аст.\nМенюро интихоб кун:",
        reply_markup=make_main_menu()
    )

# ----------------------------------------------------------
# Callback Query
# ----------------------------------------------------------
@bot.callback_query_handler(func=lambda call: True)
def callback(call):
    user_id = call.message.chat.id

    # Дидани молҳо
    if call.data == "show_products":
        bot.edit_message_text(
            "📦 Молҳои дастрас:",
            user_id,
            call.message.message_id,
            reply_markup=make_products_menu()
        )

    # Бозгашт ба менюи асосӣ
    elif call.data == "back_main":
        bot.edit_message_text(
            "Менюи асосӣ:",
            user_id,
            call.message.message_id,
            reply_markup=make_main_menu()
        )

    # Дидани сабад
    elif call.data == "view_cart":
        cart_text = make_cart_text(user_id)
        keyboard = types.InlineKeyboardMarkup()
        keyboard.add(types.InlineKeyboardButton("🛍 Харид кардан", callback_data="buy"))
        keyboard.add(types.InlineKeyboardButton("⬅️ Бозгашт", callback_data="back_main"))
        bot.edit_message_text(cart_text, user_id, call.message.message_id, reply_markup=keyboard)

    # Илова ба сабад
    elif call.data.startswith("add_"):
        pid = int(call.data.split("_")[1])
        product = products[pid]
        user_carts.setdefault(user_id, []).append(product)
        bot.answer_callback_query(call.id, f"✅ {product['name']} ба сабад илова шуд!")

    # Харид кардан
    elif call.data == "buy":
        cart = user_carts.get(user_id, [])
        if not cart:
            bot.answer_callback_query(call.id, "🛒 Сабад холӣ аст!")
            return

        total = sum([p["price"] for p in cart])
        order_text = f"🧾 Фармоиши нав аз @{call.from_user.username or call.from_user.first_name}\n\n"
        for p in cart:
            order_text += f"• {p['name']} - {p['price']} сомонӣ\n"
        order_text += f"\n💰 Ҷамъ: {total} сомонӣ"
        orders.append(order_text)

        # Тоза кардани сабад
        user_carts[user_id] = []

        bot.send_message(user_id, "✅ Фармоиши шумо қабул шуд! Админ бо шумо тамос мегирад.")
        bot.send_message(user_id, f"👑 Барои тамос: {ADMIN_USERNAME}")
        bot.send_message(user_id, "⬅️ Баргашт ба меню:", reply_markup=make_main_menu())

        # Ба админ фармоиш фиристодан
        bot.send_message(user_id, "📨 Фармоиш ба админ фиристода шуд.")
        bot.send_message(call.message.chat.id, "✅ Фармоиш сабт шуд.")
        bot.send_message(user_id, "Бозгашт ба меню:", reply_markup=make_main_menu())

    # Бахши админ
    elif call.data == "admin":
        if call.from_user.username == ADMIN_USERNAME.replace("@", ""):
            if not orders:
                bot.edit_message_text("📭 Фармоишҳои нав вуҷуд надоранд.", user_id, call.message.message_id,
                                      reply_markup=make_main_menu())
            else:
                text = "\n\n".join(orders)
                bot.edit_message_text(f"📦 Фармоишҳои нав:\n\n{text}", user_id, call.message.message_id,
                                      reply_markup=make_main_menu())
        else:
            bot.answer_callback_query(call.id, "🚫 Танҳо админ дастрасӣ дорад!")

print("✅ Бот кор мекунад...")
bot.polling(none_stop=True)
